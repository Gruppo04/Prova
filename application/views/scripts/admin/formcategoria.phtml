<?php $this->headScript()
	->appendFile('https://ajax.googleapis.com/ajax/libs/jquery/2.2.2/jquery.min.js')
	->appendFile($this->baseUrl('js/functions.js'))
	->captureStart() ?>

$(function()
{
	var actionUrl='<?= $this->url(array('controller' => 'admin', 'action' => 'validatecategoria',), 'default'); ?>';
	var formName='<?= $this->categoriaForm->getName() ?>';
	$(":input").on('blur', function(event) {
        var formElementId = $(this).attr('id');
        doValidation(formElementId, actionUrl, formName);
    });
});

<?php $this->headScript()->captureEnd() ?>

<div class="container">
    <br>
    <div class="row">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3><b>Aggiungi categoria</b></h3>
            </div>
            <div class="panel-body">
                <div role="form">
                    <?= $this->categoriaForm; ?>
                    <a class="btn btn-info" style="position: relative; left: 150px; bottom: 34px"
                       href="<?= $this->url(array(
                                                    'controller' => 'admin',
                                                    'action'     => 'amministrazione',
                                                    ), 
                                                    'default',true); 
                                                    ?>">Annulla</a>
                </div>
            </div>
        </div>
    </div>
</div>

